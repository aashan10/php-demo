@extends('layouts/app')

@section('title')
    {{ $post->title }}
@endsection

@section('body')
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="bg-white rounded-lg shadow-xl p-8 md:p-12">
        <div class="mb-8">
            <a href="/posts" class="text-blue-600 hover:underline">&larr; Back to all posts</a>
        </div>
        
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">{{ $post->title }}</h1>
        
        <div class="flex items-center text-md text-gray-500 mb-8">
            <p>By: {{ $post->user->first_name ?? 'Unknown' }} {{ $post->user->last_name ?? '' }}</p>
            <span class="mx-2">&bull;</span>
            <p>Posted on {{ date('F j, Y, g:i a', strtotime($post->created_at)) }}</p>
        </div>
        
        <div class="prose prose-lg max-w-none text-gray-800 leading-relaxed">
            {!! nl2br(htmlspecialchars($post->content)) !!}
        </div>
    </div>
</div>
@endsection
